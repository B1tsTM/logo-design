export class Contest {
  constructor(public name: string, public id?:string, public category?: string, public description?: string, public award?: number, public userName?: string, public user?: string) {

  }
}